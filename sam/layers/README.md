# Layers

The shared Lambda Layer lives at [`../../backend/layers/common`](../../backend/layers/common)
(`CommonLayer` in `template.yaml`) - not duplicated here, same reasoning as
[`../functions/README.md`](../functions/README.md).
